version: 2

run:
  timeout: 5m
  tests: true
linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
linters-settings:
  govet:
    shadow: true
issues:
  exclude-use-default: false
  max-same-issues: 0
  new: false
